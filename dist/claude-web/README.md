# Claude 웹 · 앱

`owner-lens.zip` 과 `insight-commons.zip` 을 그대로 올린다.

1. claude.ai 로그인
2. 왼쪽 아래 이름 → 설정(Settings)
3. Capabilities → Skills → 업로드

채팅에서 `/owner-lens` 또는 `/insight-commons` 로 부른다.
회사 분석 얘기를 꺼내면 자동으로 뜨기도 한다.

※ zip은 자동 생성된다. 고치려면 `skills/` 를 고치고 `python3 scripts/build.py`.
