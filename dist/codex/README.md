# GPT Codex

`AGENTS.md` 를 저장소 루트에 두면 Codex가 자동으로 읽는다.

```bash
cp AGENTS.md /경로/내프로젝트/AGENTS.md
```

이미 `AGENTS.md` 가 있으면 내용을 이어 붙인다.

전역으로 쓰려면 `~/.codex/AGENTS.md` 에 둔다.

※ 이 파일은 자동 생성된다. 고치려면 `skills/` 를 고치고 `python3 scripts/build.py`.
