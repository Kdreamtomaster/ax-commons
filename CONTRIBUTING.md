# 기여하기

되돌려 주는 흐름이 있어야 이 프로젝트가 성립한다.
한 사람의 원장은 몇 건짜리 경험칙이다. 여럿이 모아야 폐쇄 벤치마크의 대체물이 된다.

---

## 제일 필요한 것

### 1. 임기 게이트가 실제로 맞았는지 ★

`safe_ratio: 0.6` 은 **경험칙이지 검증된 값이 아니다.**
쓰고 나서 알려주면 그게 이 프로젝트에서 제일 값진 기여다.

이슈에 이렇게 적어 주면 된다.

```
조건: 전문 경영 / 담당 임원 잔여 임기 약 2년 / 제조업 중견
게이트 판정: 통과 (회수 1년, 임기 2년 → 0.5)
실제 결과: 통과됐는데 관심이 없었음
추정 원인: 회수는 짧았지만 담당자 KPI와 무관했음
제안: 임기만이 아니라 KPI 연결 여부도 봐야 할 듯
```

**틀렸다는 보고가 맞았다는 보고보다 값지다.** 조건을 좁힐 수 있게 해 주기 때문이다.

### 2. 산업별 마진 스프레드

`skills/owner-lens/references/tuning.md` 의 `margin_reference` 를 채운 것.
**출처와 연도를 반드시 함께** 적는다. 숫자는 낡는다.

### 3. 신호 해석 규칙

채용공고나 공시에서 "이건 이렇게 읽으면 된다"를 발견했으면.
`skills/owner-lens/references/pipeline.md` 의 S1 표에 추가한다.

### 4. 로컬 모델 실측

`dist/local-models/README.md` 의 표가 아직 검증되지 않았다.
어떤 모델이 규칙을 지켰는지 / 못 지켰는지, 몇 B부터 쓸 만한지.

### 5. 번역 · 새 플랫폼 어댑터

---

## 인사이트 원장을 내놓을 때

`insight-commons` 의 규칙을 그대로 따른다.

- **다섯 칸을 다 채운다** — 조건 / 관찰 / **메커니즘** / 전이 범위 / 검증 상태
- **메커니즘 없는 항목은 받지 않는다.** 설명 없는 상관은 조건이 바뀌면 깨진다
- **탈식별화 점검을 통과해야 한다** — "이 문장만 보고 어디인지 좁혀지는가?"
  좁혀지면 숫자를 관계로 바꾸거나 뺀다
- **`[반증됨]` 항목도 함께 낸다.** 빼면 생존 편향이 된다
- 검증 상태를 부풀리지 않는다. 1회는 1회다

---

## 코드·문서 고치기

### 원본은 `skills/` 하나뿐이다

```
skills/        ★ 여기만 고친다
dist/          자동 생성. 직접 고치지 않는다
```

고친 뒤 반드시 빌드를 돌리고 결과를 함께 커밋한다.

```bash
python3 scripts/build.py
```

빌드를 안 돌리면 8벌이 서로 어긋난다.


### 공개 전 검사

PR을 보내기 전, 그리고 병합 전에 돌린다.

```bash
python3 scripts/scan-private.py
```

내 고객사명까지 검사하려면 저장소 루트에 `.private-terms.txt` 를 만들고
한 줄에 하나씩 적는다. **이 파일은 .gitignore 에 있어서 커밋되지 않는다.**
실제 이름을 적어도 안전하다.
### 고치면 다른 도구가 되는 부분

아래는 이 도구를 이 도구이게 하는 부분이다.

- 뻔한 답 게이트
- 근거 등급 표기
- `[불가]` 판정을 반드시 남기는 규칙
- "남은 모름"을 필수 산출로 두는 규칙
- **반박 안내를 필수로 두는 규칙**
- 인사이트 원장의 특정 가능성 점검

**고치는 것도 자유다.** 다만 그때는 다른 이름을 붙이기 바란다.
이건 취향 문제가 아니라, 이 규칙들이 빠지면 도구의 성격이 반대가 되기 때문이다.

---

## PR 보낼 때

- 한 PR에 한 가지만
- 문서 수정은 이유를 한 줄로
- 규칙을 바꾸는 제안이면 **왜 지금 규칙이 문제인지**를 사례로 보인다
- 한국어 문서는 단문·`~다` 종결로 쓴다. 영어 문서는 평이한 영어로

---

## 행동 규칙

- **권위를 만들지 않는다.** 이 프로젝트에는 최종 판정자가 없다
- 반박은 환영이고 인신공격은 아니다
- 남의 원장 항목을 반박할 때는 **조건을 좁히는 방향**으로 한다. 지우자고 하지 않는다
- 모른다고 말하는 것이 기본값이다

---

## 라이선스

CC0. 기여하면 그 기여물도 퍼블릭 도메인이 된다.
PR을 보내는 것으로 이에 동의하는 것으로 본다.
