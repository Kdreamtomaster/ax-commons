# 결정 기록

**왜 이렇게 되어 있는가.**

새로 온 사람이 "이건 왜 이래요?"라고 물었을 때 답이 있어야 한다.
답이 없으면 매번 같은 논의를 다시 한다.

> **소급해서 적었다는 점을 밝힌다**
> 아래 대부분은 결정 당시에 기록되지 않았다. 나중에 기억과 커밋 이력으로 복원했다.
> **부정확할 수 있다.** 틀린 게 있으면 이슈로 알려 달라.
> 이게 [GOVERNANCE.md](GOVERNANCE.md)의 「이슈에 없으면 없던 일이다」가 필요한 이유다.

---

## 쓰는 법

새 결정은 **맨 아래에 추가한다.** 번호는 이어 붙인다.

```markdown
## D-00N. 한 줄 제목

- **정한 것**
- **왜** —
- **버린 것** —
- **되돌리려면** —
- 근거: 이슈 #N / 커밋 해시
```

**「버린 것」을 반드시 적는다.** 트레이드오프가 없는 결정은 결정이 아니다.
결정을 되돌릴 때는 지우지 않고 **아래에 뒤집는 항목을 추가한다.**

---

## D-001. 라이선스를 CC0로 한다

- **정한 것** MIT·Apache-2.0이 아니라 CC0. 저작자 표시 의무도 없앴다
- **왜** 이 프로젝트의 목적은 확산이지 소유가 아니다.
  만든 사람에게 묶이면 버스 계수 1이 그대로 프로젝트의 수명이 된다.
  **회사가 사내 문서에 넣을 때 법무 검토가 필요 없어야 한다**
- **버린 것** 출처 표시. 어디서 왔는지 모른 채 퍼져도 막을 방법이 없다.
  기여자 이름이 남는다는 보상도 없다
- **되돌리려면** 되돌릴 수 없다. 이미 배포된 것은 CC0로 남는다
- 근거: `LICENSE`, [MATURITY.md](MATURITY.md) 「버스 계수 1」

## D-002. 원본은 `skills/` 하나만 둔다

- **정한 것** `skills/` 를 유일한 원본으로 하고 `scripts/build.py` 가 8개 배포본을 만든다
- **왜** 클로드 웹·코드·코덱스·오픈클로·에르메스·로컬 모델을 다 지원하려니
  같은 규칙이 8벌 필요했다. 손으로 맞추면 반드시 어긋난다
- **버린 것** 플랫폼별 최적화. 어댑터가 형식만 바꾸고 내용은 못 바꾼다
- **부작용** [#23](https://github.com/Kdreamtomaster/ax-commons/issues/23)에서 드러났다.
  **원본 하나가 어긋나면 여덟 군데가 동시에 어긋난다.** 빌드가 오류도 잘 복제한다
- **되돌리려면** 어댑터별 덮어쓰기 층을 두면 된다. 대신 어긋남 검사가 어려워진다
- 근거: `scripts/build.py`, `python3 scripts/build.py --check`

## D-003. 생성물인 `dist/` 를 저장소에 커밋한다

- **정한 것** 자동 생성물인데도 git에 넣는다
- **왜** 대상 독자가 개발자만이 아니다. **클론·빌드 없이 파일을 복사해 붙일 수 있어야 한다**
- **버린 것** 저장소가 깔끔하지 않다. PR 변경량이 커진다. 빌드를 안 돌린 PR이 섞일 수 있다
- **되돌리려면** `dist/` 를 gitignore 하고 릴리스 첨부로 옮긴다.
  단 「설치 없이 바로 쓴다」가 깨진다
- 근거: [CONTRIBUTING.md](CONTRIBUTING.md) 「원본은 `skills/` 하나뿐이다」

## D-004. 첫 질문을 여섯 갈래에서 세 갈래로 줄인다

- **정한 것** 시작 질문 R0의 선택지를 6개(A~F)에서 3개(① 파는 쪽 / ② 사는 쪽 / ③ 일하는 쪽)로
- **왜** 여섯 개는 **읽고 고르는 데 시간이 걸린다.** 첫 질문에서 이탈한다.
  그리고 D(자료 재구성)·F(대립 검토)는 애초에 자리가 아니라 **시점**으로 정해지는 것이라
  같은 층에 놓는 것 자체가 틀렸다
- **버린 것** 사용자가 모드를 직접 고르는 자유. 이제 D·F는 진행 중에 도구가 판단한다
- **되돌리려면** R0에 「기타」를 추가한다. 다만 선택지 수가 다시 늘어난다
- 근거: 커밋 `ux: 첫 질문을 여섯 갈래에서 세 갈래로 줄임`

## D-005. 모드 문자(A~F)를 사용자에게 보이지 않는다

- **정한 것** 내부 이름은 A~F로 두되 화면에는 ① ② ③ 만 나온다
- **왜** 문자는 **아무 뜻이 없다.** "모드 E입니다"는 사용자에게 정보가 아니다.
  내부 이름은 문서·이슈에서 짧게 가리키는 데만 쓴다
- **버린 것** 없다. 다만 문서에서 `모드 A` 라고 쓰는 습관이 남아 여러 번 정리해야 했다
- 근거: [docs/glossary.md](docs/glossary.md) 「모드 이름」

## D-006. 세 갈래에 당사자 이름을 괄호로 병기한다

- **정한 것** `① 파는 쪽 (AX 회사)` `② 사는 쪽 (AX 추진 조직)` `③ 일하는 쪽 (현업)`
- **왜** 「파는 쪽」만 쓰면 영업으로 읽힌다. 「AX 회사」만 쓰면 자기가 어디인지 헷갈린다.
  **쉬운 말이 앞, 정확한 말이 뒤.** 둘 다 필요하다
- **버린 것** 줄이 길어진다. 좁은 화면에서 줄바꿈이 생긴다
- 근거: 커밋 `ux: 세 갈래에 당사자 이름을 괄호로 병기`

## D-007. 예시는 전부 가공 데이터로 만든다

- **정한 것** 「(주)예시정밀」 하나를 만들고 5건 모두 그 회사로. 실제 회사·수치를 쓰지 않는다
- **왜** 이 도구는 비식별화를 필수로 요구한다.
  **예시에 실제 회사를 쓰면 그 규칙을 스스로 어기는 셈이다**
- **버린 것** 현실성. 가공 데이터라 수치가 예쁘게 맞아떨어진다. 실제는 이렇게 깔끔하지 않다.
  그리고 5건이 전부 같은 회사·같은 산업이라 **관점이 하나다**
- **남은 문제** [MATURITY.md](MATURITY.md) 「기여자 동질성」이 이 결정의 대가다
- 근거: `examples/`, `scripts/scan-private.py`

## D-008. 미검증 항목을 지우지 않고 이슈로 공개한다

- **정한 것** 확인 안 된 값(`safe_ratio 0.6`, 로컬 모델 표 등)을 빼지 않고 남기되
  **미검증임을 밝히고 이슈 번호를 붙인다**
- **왜** 빼면 저장소가 완성돼 보인다. 완성돼 보이면 아무도 검증하지 않는다.
  이 도구가 산출물마다 「반박 안내」를 붙이는 것과 같은 규칙을 저장소에 적용한 것이다
- **버린 것** 첫인상. **미완성으로 보인다**
- 근거: 커밋 `docs: 미검증 항목에 이슈 번호 연결 (#1~#9)`

## D-009. 자극적인 표현을 걷어내고 현업 관련은 긍정형으로 쓴다

- **정한 것** 부정 어휘를 완화하고, 현업을 다루는 문장은 **적극 반영하고자 한다**는 방향으로
- **왜** 이 문서를 읽는 사람 중에 **비판 대상이 될 수 있는 자리에 있는 사람이 있다.**
  읽자마자 방어에 들어가면 내용이 전달되지 않는다.
  「현업의 목소리가 작다」는 진단이고, 「현업 의견을 적극 반영한다」는 방향이다.
  방향으로 쓰는 쪽이 실제로 움직인다
- **버린 것** 문제의 날이 무뎌진다
- **부작용** 이때 요청을 **어조 문제로만 받았다.** 「정확한가」는 점검하지 않았다.
  D-010의 용어 충돌이 이 단계를 그대로 통과했다
- 근거: 커밋 `docs: 전체 윤문 — 표현을 부드럽게, 프레임을 긍정으로`

## D-010. 「자리」를 「결재선」으로, 「팔린다」를 「통과」로 바꾼다

- **정한 것** 결재 권한을 뜻하던 `자리`·`레벨`을 **결재선**으로 통일.
  임기 게이트 판정을 `팔린다/안 팔린다` 에서 **통과/통과되지 않음**으로
- **왜** 두 가지가 겹쳤다.
  ① `자리` 가 ①②③(입장)과 결재 권한 두 뜻으로 쓰여 **프롬프트 안에서 정면 충돌**했다.
  ② `팔린다` 는 ① 파는 쪽 전용 언어인데 **공용 규칙**에 들어가 있었다.
  세 자리를 다 지원한다고 써 놓고 규칙 언어가 한쪽 것이었다
- **버린 것** 「결재선」이 「자리」보다 딱딱하다
- **왜 중요한가** 여기서 만드는 건 AI에게 주는 규칙이다.
  **용어 충돌은 문체 문제가 아니라 동작 문제다**
- 근거: [#23](https://github.com/Kdreamtomaster/ax-commons/issues/23),
  [디스커션 #18](https://github.com/Kdreamtomaster/ax-commons/discussions/18),
  [docs/glossary.md](docs/glossary.md)

## D-011. Apache 인큐베이터 방식을 부분만 가져온다

- **정한 것** 성숙도 자기평가 · 알려진 위험 · 게으른 합의 · 기여자 경로 네 가지만.
  릴리스 투표 · IP 정리 · CLA · 재단 보고 · 상표는 뺐다
- **왜** 뺀 것들은 **여기 해당이 없다.** 릴리스가 없고, CC0라 IP 정리가 필요 없다.
  형식만 갖추면 지키지도 않을 절차가 문서에 남는다.
  [MATURITY.md](MATURITY.md)의 「방법론 과몰입」이 그 위험이다
- **가장 크게 고친 것** Apache는 기여로 **권위**를 쌓는 구조다.
  이 저장소는 「권위가 애초에 존재하지 않는다」고 적었다.
  그대로 두면 부딪쳐서 **판단의 권위(없다)와 운영의 권한(있다)으로 갈랐다**
- **버린 것** 검증된 절차를 통째로 쓸 때의 안정성. 골라 쓴 조합은 아무도 안 해 봤다
- **되돌리려면** 안 맞는 절차는 이슈로 말하고 뺀다. 억지로 유지하지 않는다
- 근거: [MATURITY.md](MATURITY.md), [GOVERNANCE.md](GOVERNANCE.md)
